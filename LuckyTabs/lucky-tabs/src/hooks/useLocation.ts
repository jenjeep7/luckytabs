// Re-export the location context and hook for easier imports
export { useLocation, LocationProvider } from '../context/LocationContext';
export type { LocationContextType } from '../context/LocationContext';

// If you need additional location-related hooks, add them here
